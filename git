// comandos git:

git init // cria repositório local
git config user.name "NomeUsuario" // define o usuário
git config user.email "emailUsuario" // define o email

git remote add origin http://... // aponta para o repositório remoto
git remote -v // visualiza repositório criado
git remote rm origin // remove o reporitório remoto

git status // mostra os arquivos que não estão no repositório
git add <file> // adiciona um arquivo ao repositório
git add . // adiciona todas as modificações
git commit -m "mensagem" // comita (salva) as modificações feitas

git push origin master // envia as modificações ao repositório remoto

git clone <url> // copia arquivos do repositório remoto para um novo local (uma pasta qualquer)
ls // se quiser ver a lista de arquivos clonados

git pull origin master // atualiza repositório local com alterações presentes no remoto

------------------------------

cd <folder> // change directory
mkdir <newFolder> // make directory
pwd // print working directory
ls // list: mostra os arquivos presentes em uma pasta
ls -lha